# ReportLab is used both at build time and at runtime.
#
# Build time:
#   python scripts/build_syllabus_pdf.py
#
# The generated syllabus PDF is committed to the repo and served as a static file.
#
# Runtime:
#   app/app.py imports ReportLab to generate Study Notes PDFs for users.
#
# Keep this version aligned with requirements.txt.

reportlab==4.4.4
