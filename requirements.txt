# AI Learning Copilot — Python 3.11 (both machines)
#
#   python3.11 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Every version below is pinned on purpose. This exact set was installed together and
# smoke-tested on 2026-08-05 (Python 3.11.15, macOS): all project imports resolve, a
# Chroma persist/query round-trip works, and `pip check` is clean.
#
# NEVER install a langchain-* package unpinned. `pip install langchain` today gives you
# 1.x, which breaks every pattern the course taught (AgentExecutor, initialize_agent,
# langchain.chains). If you need something new: pin it here, test it, tell the other person.
#
# requirements.lock.txt holds the full 200-package freeze for byte-identical reproduction.

# --- LangChain (0.2 line — the API taught in demos_ai_eng) ---
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-openai==0.1.25
langchain-text-splitters==0.2.4
langsmith==0.1.147

# --- Vector store ---
# langchain-chroma caps chromadb below 0.6. 0.5.3 is a pure-python wheel (py3-none-any),
# so nothing compiles on either Apple Silicon or Felipe's Intel Mac.
chromadb==0.5.3
langchain-chroma==0.1.4

# --- LLM / embeddings — API only, no local models (see PLAN.md section 7) ---
openai==1.109.1
tiktoken==0.13.0

# --- Ingestion ---
webvtt-py==0.5.1
yt-dlp==2026.7.4
pypdf==6.14.2
beautifulsoup4==4.15.0
requests==2.34.2

# --- App ---
streamlit==1.61.1
reportlab==5.0.0
# starlette is a TRANSITIVE dep of streamlit, pinned here deliberately — do not remove.
#
# Why: streamlit 1.61.0 declared `starlette<2`, so nothing stopped a fresh install from
# resolving starlette anywhere in that wide range. streamlit 1.61.1 then TIGHTENED it to
# `starlette<1.4.0`, i.e. the maintainers explicitly excluded 1.4.x. Pinning both sides
# means we sit on a pair the maintainers still declare compatible, instead of letting the
# resolver wander into the excluded range on someone's next clean install.
#
# Honest scope of this change: with a trivial app, streamlit 1.61.0 + starlette 1.4.0 both
# passes `pip check` AND serves HTTP 200 — the breakage Felipe hit locally was NOT
# reproducible here, so treat this as removing an unpinned-drift risk rather than as a fix
# for a proven runtime bug. 1.3.1 is the newest starlette allowed by streamlit 1.61.1.
starlette==1.3.1
python-dotenv==1.2.2
pydantic==2.13.4

# --- Notebooks / analysis ---
pandas==2.3.3
numpy==1.26.4
jupyter==1.1.1
ipykernel==7.3.0

# --- Pinned to silence chromadb 0.5.3's telemetry errors against newer posthog ---
posthog==2.5.0
